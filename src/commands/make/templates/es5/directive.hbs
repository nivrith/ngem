(function () {
    'use strict';

    angular
    .module ('{{name.camel}}')
    .directive ('{{name.camel}}directive', {{name.camel}}directive);

    /** @ngInject */
    function {{name.camel}}directive() {
        // Usage:
        //     <directive></directive>
        // Creates:
        //
        var directive = {
            link: link,
            restrict: 'EA'
        };
        return directive;

        function link(scope, element, attrs) {
        }
    }

})();
